# Pedersen Commitments: Building the Sealed Envelope

*Chapter 11 — The Bedrock · the discrete-log commitment that closes the Ch 2 spiral*
*Target depth: rigorous · stratum: Group theory & number-theoretic hardness (where security lives)*

![A prime-order group drawn as a clock with generators g and h, the commitment C, and the hiding orbit of Com(m,r) for fixed m and varying r](figures/pedersen-commitment.svg)

*Figure — the order-1019 subgroup of (ℤ/2039ℤ)\* drawn as a clock. Green `g = 9` and purple `h = 726` are the two public generators; red `C = g⁴²·h⁸⁰⁰ = 71` is the commitment; the blue points are the hiding orbit `{ Com(42, r) : r ∈ {0,1,800,17,999} } = {1641, 590, 71, 143, 834}` — the same message, smeared across the group by the randomness.*

> **Animation:** [`animations/pedersen-commitment.mp4`](animations/pedersen-commitment.mp4) — first **hiding**: with the message `m = 42` fixed, the randomness `r` is swept and the committed point walks all around the group (it is uniform over the whole ring); then **binding**: for a fixed `(m, r)` the commitment is the single point `C = 71`, and a second opening to a different message would reveal `logₘ(h)` — the discrete log. Displayed values (`p = 2039`, `q = 1019`, `g = 9`, `h = 726`, `m = 42`, `r = 800`, `C = 71`) match the Sage manifest with no drift.

---

> ### Math you'll need (sidebar)
> Before this section you should be comfortable with:
> - **Cyclic groups** — a group generated by one element; the *order* of an element; the order-`q` subgroup of `(ℤ/pℤ)*`.
> - **Prime-order groups** — when `q` is prime, *every* non-identity element is a generator. We use a **safe prime** `p = 2q + 1` so the quadratic residues form a subgroup of prime order `q`.
> - **The Discrete Logarithm assumption** — given `g` and `h = gᵃ` in such a group, recovering `a` is infeasible. This single bet is what binding rests on.
> - **Modular arithmetic & modular inverses** — group elements multiply **mod `p`**; exponents live and invert **mod `q`**. Keeping the two moduli straight is the whole game.
> - **Basic probability** — a uniform distribution over a finite set leaks nothing about its source.
>
> *Carried in from Ch 2:* the **sealed envelope** — a commitment with *hiding* (it conceals the value until opened) and *binding* (you can't swap the slip after sealing), named there but deliberately **not built**. *Carried in from Ch 9–10:* hiding and binding given rigorous definitions; the *knowledge extractor* met as the Schnorr/Sigma "two transcripts" move. Here we finally **build** the envelope.

---

## Pre-rigorous — one point on a clock, two promises

Back in Chapter 2 you met a sealed envelope: write a number on a slip, seal it, hand it over. Two promises came with it — the envelope **hides** the number until you open it, and once sealed you **can't swap** the slip. We never built the envelope; we only described it. Now we build it.

Look at the figure. A prime-order group is just a clock face, and we paint two public marks on the rim: `g` and `h`. To commit to a message `m`, you also roll a secret die `r`, and you place a **single point** on the clock. That point is your sealed envelope.

The picture shows the magic. Keep the message `m` fixed and just **re-roll `r`**: the blue point jumps all around the clock — over every possible roll it lands on every position equally often. So from the sealed point alone, an onlooker cannot tell which message you started from: the die has *smeared* it across the whole ring. That is **hiding**, and you can *see* it.

Now the other promise. For a fixed message and a fixed die, there is exactly **one** point. To later claim a *different* message, you'd have to find a second die that lands on the very same point — and the only way to do that is to know the secret relationship between the two marks `g` and `h`, which nobody does. That is **binding**.

One picture, both promises: a die that smears the message everywhere (hiding) yet pins down a single point per `(message, die)` pair (binding).

*(No formulas yet beyond naming the marks `g`, `h` and the roll `r` — the "why" comes before the notation.)*

## Rigorous — earn the two properties

Work in a cyclic group of **prime order `q`** — concretely the order-`q = 1019` subgroup of `(ℤ/pℤ)*` for the safe prime `p = 2039`, so every non-identity element generates. Fix two generators `g` and `h`; crucially, `h = gᵃ` for some `a = logₘ(h)` that **nobody knows**. The **Pedersen commitment** to a message `m ∈ ℤ/qℤ` with randomness `r ∈ ℤ/qℤ` is

> **`C = Com(m, r) = gᵐ · hʳ`.**

**Hiding (perfect, information-theoretic).** Fix any `m`. As `r` ranges over `ℤ/qℤ`, the term `hʳ` ranges over the *entire* group, so `r ↦ gᵐ · hʳ` is a **bijection** onto the group: `C` is uniformly distributed and statistically **independent of `m`**. No assumption is used — an *unbounded* adversary still learns nothing. Equivalently, for **any** target message `m′` there is exactly one `r′ = r + (m − m′)·a⁻¹ (mod q)` with `gᵐ′ · hʳ′ = C`, so `C` is consistent with *every* message.

**Binding (computational, == discrete log).** Suppose an adversary outputs two valid openings `(m, r) ≠ (m′, r′)` of the same `C`. Then `gᵐ hʳ = gᵐ′ hʳ′`, so `g^{m−m′} = h^{r′−r}`, giving

> **`logₘ(h) = a = (m − m′)·(r′ − r)⁻¹ (mod q)`**  (and `r′ ≠ r` whenever `m ≠ m′`).

Producing two openings is *exactly* solving the discrete log of `h` base `g`. Under the **Discrete Logarithm assumption** no efficient adversary can, so binding holds against *bounded* adversaries only — it is computational.

This destroys the tempting bad intuitions, explicitly:

1. **"`gᵐ` leaks `m`" is false.** The blinding factor `hʳ` smears `C` uniformly over the whole group; `C` is independent of `m`.
2. **"Hiding rests on DLog, like binding" is false.** Hiding is a *counting* fact (no assumption); only **binding** rests on DLog.
3. **"`g` and `h` are arbitrary" is false.** Their *unknown relative log* `a` **is** the binding security — knowing `a` lets you open any `C` to any message.
4. **"Only computational means weak" is false.** An attacker who breaks binding has solved discrete log — the chapter's whole hardness bet.

For our concrete group: `g = 9`, `h = 726`, `m = 42`, `r = 800` give `C = 71`. The alternate opening `m′ = 100` forces `r′ = 800 + (42−100)·571⁻¹ (mod 1019) = 186`, and `g¹⁰⁰ · h¹⁸⁶ = 71 = C` (perfect hiding). Feeding those two openings to the extractor returns `a = (42−100)·(186−800)⁻¹ (mod 1019) = 571 = logₘ(h)` (binding == DLog).

> **Note (which property is unconditional).** Hiding is **perfect** — proved by counting, no assumption, safe against an unbounded adversary. Binding is **computational** — it reduces to discrete log. You cannot make *both* perfect for one group element: the very alternate opening that proves perfect hiding is what an all-powerful adversary would use to break binding.

## Post-rigorous — both halves at once

Rebuild the intuition on the rigor. The clock that "smears the message everywhere" **is** the bijection `r ↦ gᵐ · hʳ` onto the whole group — perfect hiding is the statement that this map is a uniform shuffle, needing no assumption. The "single point per `(m, r)`, and you can't fake a second die" **is** the extractor: a second opening hands you `logₘ(h)`, which DLog says you cannot have. Pair each picture with its statement:

| Picture (heuristic) | Formal statement |
|---|---|
| Re-roll `r` and the point jumps everywhere on the clock, equally often | `r ↦ gᵐ·hʳ` is a bijection of `ℤ/qℤ` onto the group ⇒ `C` uniform, independent of `m` ⇒ **perfect hiding** |
| One fixed point per `(m, r)`; a second die needs the secret link of `g, h` | two openings ⇒ `g^{m−m′} = h^{r′−r}` ⇒ `logₘ(h) = (m−m′)(r′−r)⁻¹` ⇒ **binding == DLog** |
| One promise is free, the other is a bet — you can't have both unbreakable | hiding unconditional (counting); binding conditional (hardness); the perfect-hiding opening is the binding attack |
| Marks `g=9`, `h=726`: msg 42, die 800 → point 71; msg 100, die 186 → **same** 71 | `g⁴²h⁸⁰⁰ = 71` and `g¹⁰⁰h¹⁸⁶ = 71` (Sage-verified); extractor on these returns `logₘ(h) = 571` |

The asymmetry is now **inevitable**: hiding is unconditional because it is a counting fact about a finite group; binding is conditional because it is a hardness fact about that group.

This is the construction that finally builds Ch 2's sealed envelope — and it is the base of a whole line. Stack many generators and you get the **vector Pedersen commitment** `hʳ · ∏ᵢ gᵢ^{mᵢ}`, whose succinct opening is the **inner-product argument (IPA / Bulletproofs)**, and whose additive homomorphism `Com(m₁,r₁)·Com(m₂,r₂) = Com(m₁+m₂, r₁+r₂)` powers range proofs and proof-of-reserves.

Keep one boundary sharp: this is a **discrete-log, transparent, additively-homomorphic** commitment — perfectly hiding, computationally binding. It is **not** a KZG / pairing commitment (computationally binding under q-SDH, needing a trusted setup). Ch 11 builds both; conflating their assumptions and setups corrupts the security map.

---

> ### Rediscover it (you could have invented this)
> Suppose you know only this: you have a prime-order group with a generator `g`, and you can publish a second element `h` whose discrete log base `g` you do **not** know. You want to seal a number `m` so that (a) the seal reveals nothing about `m`, yet (b) you cannot later claim a different `m`.
>
> Pick fresh randomness `r` and publish `C = gᵐ · hʳ`.
> - **Hiding** falls out by counting: for fixed `m`, as `r` ranges over the exponents, `hʳ` is uniform over the whole group, so `C` is uniform and independent of `m` — no assumption needed.
> - **Binding** falls out by algebra: two openings `(m, r) ≠ (m′, r′)` give `g^{m−m′} = h^{r′−r}`, i.e. `logₘ(h) = (m−m′)(r′−r)⁻¹` — which you can't have.
>
> You have just rediscovered the **Pedersen commitment**: perfectly hiding, computationally binding, with binding == discrete log. You derived it; you did not receive it.

---

## Check yourself (comprehension set)

**Recall.** Write down the Pedersen commitment to a message `m` with randomness `r` in a prime-order group with public generators `g` and `h`, and name its two security properties and which one is unconditional.
> *Answer:* `Com(m, r) = gᵐ · hʳ`. It is **perfectly (information-theoretically) hiding** and **computationally binding**. Hiding is the unconditional one — it needs no assumption; binding rests on the discrete-log assumption between `g` and `h`.
> *If you miss this →* revisit **the Discrete Logarithm assumption** (given `g` and `h = gᵃ`, recovering `a` is infeasible).

**Apply.** In the order-1019 subgroup of `(ℤ/2039ℤ)*` with `g = 9`, `h = 726`, you publish `C = g⁴²·h⁸⁰⁰ = 71`. Does `C` leak the message `m = 42`? Exhibit a different message `m′ = 100` that the same `C` also opens to, and give the randomness `r′`.
> *Answer:* `C` does not leak `m`: for the fixed message, `r ↦ g⁴²·hʳ` is a bijection onto all 1019 group elements, so `C = 71` is uniform and independent of `m`. The same `C = 71` opens to `m′ = 100` with `r′ = r + (m − m′)·a⁻¹ (mod 1019)`, where `a = logₘ(h) = 571`; this gives `r′ = 186`, and `g¹⁰⁰·h¹⁸⁶ ≡ 71 (mod 2039)`.
> *If you miss this →* revisit **modular arithmetic and modular inverses in `ℤ/pℤ` and `ℤ/qℤ`**.

**Transfer.** Suppose an adversary produces two valid openings `(m, r)` and `(m′, r′)` of the **same** commitment `C` with `m ≠ m′`. What have they necessarily computed, and what does this say about the assumption binding rests on — and why is hiding *not* in the same boat?
> *Answer:* From `gᵐhʳ = gᵐ′hʳ′` you get `g^{m−m′} = h^{r′−r}`, hence `logₘ(h) = (m − m′)·(r′ − r)⁻¹ (mod q)`. Two openings force the discrete log of `h` base `g`: breaking binding == solving DLog, which is why binding is only computational. Hiding is different — proved by counting (`r ↦ Com(m,r)` is uniform), needs no assumption, and holds even against an unbounded adversary, so it is perfect.
> *If you miss this →* revisit **the Discrete Logarithm assumption**.

**Rediscover.** You want to seal a number `m` so that the seal reveals nothing yet cannot be reopened to a different `m`. You have a prime-order group with generator `g` and may publish a second element `h` whose discrete log base `g` you do not know. Derive a commitment from scratch and argue both properties.
> *Answer:* Publish `C = gᵐ · hʳ` for fresh random `r`. **Hiding:** for fixed `m`, `hʳ` is uniform over the whole group, so `C` is uniform and independent of `m` (no assumption). **Binding:** two openings give `g^{m−m′} = h^{r′−r}`, so you'd know `logₘ(h)`; nobody does. That is the Pedersen commitment.
> *If you miss this →* revisit **prime-order groups** (every non-identity element is a generator; safe primes `p = 2q + 1`).

---

*Verification: all numeric claims match the Sage manifest (`p 2039`, `group_order 1019`, `g 9`, `h 726`, `m 42`, `r 800`, `commitment 71`, alternate opening `m′ 100 / r′ 186 → 71`, extracted `logₘ(h) = 571`); the manim scene's displayed values pass `validate_scene_values` with no drift; scorecard PASS. Figure and animation generated correct-by-construction by `scripts/recipes/pedersen-commitment.sage` and `scripts/scenes/pedersen-commitment.py`.*
